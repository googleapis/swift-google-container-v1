// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// ContainerdConfig contains configuration to customize containerd.
public struct ContainerdConfig: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// PrivateRegistryAccessConfig is used to configure access configuration
  /// for private container registries.
  public var privateRegistryAccessConfig: ContainerdConfig.PrivateRegistryAccessConfig? = nil

  /// Optional. WritableCgroups defines writable cgroups configuration for the
  /// node pool.
  public var writableCgroups: ContainerdConfig.WritableCgroups? = nil

  /// RegistryHostConfig configures containerd registry host configuration.
  /// Each registry_hosts represents a hosts.toml file.
  /// At most 25 registry_hosts are allowed.
  public var registryHosts: [ContainerdConfig.RegistryHostConfig] = []

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `ContainerdConfig`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ContainerdConfig().with { $0.privateRegistryAccessConfig = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let privateRegistryAccessConfig = CodingKeys(stringValue: "privateRegistryAccessConfig")
    static let writableCgroups = CodingKeys(stringValue: "writableCgroups")
    static let registryHosts = CodingKeys(stringValue: "registryHosts")

    static let _knownKeys: Set<Swift.String> = [
      "privateRegistryAccessConfig",
      "writableCgroups",
      "registryHosts",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.privateRegistryAccessConfig = try container.decodeIfPresent(
      ContainerdConfig.PrivateRegistryAccessConfig.self, forKey: .privateRegistryAccessConfig)
    self.writableCgroups = try container.decodeIfPresent(
      ContainerdConfig.WritableCgroups.self, forKey: .writableCgroups)
    if let value = try container.decodeIfPresent(
      [ContainerdConfig.RegistryHostConfig].self, forKey: .registryHosts)
    {
      self.registryHosts = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encodeIfPresent(
      self.privateRegistryAccessConfig, forKey: .privateRegistryAccessConfig)
    try container.encodeIfPresent(self.writableCgroups, forKey: .writableCgroups)
    try container.encode(self.registryHosts, forKey: .registryHosts)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// PrivateRegistryAccessConfig contains access configuration for
  /// private container registries.
  public struct PrivateRegistryAccessConfig: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Private registry access is enabled.
    public var enabled: Swift.Bool = Swift.Bool()

    /// Private registry access configuration.
    public var certificateAuthorityDomainConfig:
      [ContainerdConfig.PrivateRegistryAccessConfig.CertificateAuthorityDomainConfig] = []

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `PrivateRegistryAccessConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PrivateRegistryAccessConfig().with { $0.enabled = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let enabled = CodingKeys(stringValue: "enabled")
      static let certificateAuthorityDomainConfig = CodingKeys(
        stringValue: "certificateAuthorityDomainConfig")

      static let _knownKeys: Set<Swift.String> = [
        "enabled",
        "certificateAuthorityDomainConfig",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .enabled) {
        self.enabled = value
      }
      if let value = try container.decodeIfPresent(
        [ContainerdConfig.PrivateRegistryAccessConfig.CertificateAuthorityDomainConfig].self,
        forKey: .certificateAuthorityDomainConfig)
      {
        self.certificateAuthorityDomainConfig = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.enabled, forKey: .enabled)
      try container.encode(
        self.certificateAuthorityDomainConfig, forKey: .certificateAuthorityDomainConfig)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// CertificateAuthorityDomainConfig configures one or more fully qualified
    /// domain names (FQDN) to a specific certificate.
    public struct CertificateAuthorityDomainConfig: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// List of fully qualified domain names (FQDN).
      /// Specifying port is supported.
      /// Wildcards are NOT supported.
      /// Examples:
      /// - `my.customdomain.com`
      /// - `10.0.1.2:5000`
      public var fqdns: [Swift.String] = []

      /// Certificate access config. The following are supported:
      /// - GCPSecretManagerCertificateConfig
      public var certificateConfig: OneOf_CertificateConfig? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `CertificateAuthorityDomainConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = CertificateAuthorityDomainConfig().with { $0.fqdns = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let fqdns = CodingKeys(stringValue: "fqdns")
        static let gcpSecretManagerCertificateConfig = CodingKeys(
          stringValue: "gcpSecretManagerCertificateConfig")

        static let _knownKeys: Set<Swift.String> = [
          "fqdns",
          "gcpSecretManagerCertificateConfig",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent([Swift.String].self, forKey: .fqdns) {
          self.fqdns = value
        }

        var certificateConfig: OneOf_CertificateConfig? = nil
        let certificateConfigCheckAndSet = {
          if certificateConfig != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'certificateConfig'"))
          }
          certificateConfig = $0
        }
        if let gcpSecretManagerCertificateConfig = try container.decodeIfPresent(
          ContainerdConfig.PrivateRegistryAccessConfig.CertificateAuthorityDomainConfig
            .GCPSecretManagerCertificateConfig?.self, forKey: .gcpSecretManagerCertificateConfig)
        {
          try certificateConfigCheckAndSet(
            .gcpSecretManagerCertificateConfig(gcpSecretManagerCertificateConfig))
        }
        self.certificateConfig = certificateConfig
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.fqdns, forKey: .fqdns)

        if let choice = self.certificateConfig {
          switch choice {
          case .gcpSecretManagerCertificateConfig(let value):
            try container.encode(value, forKey: .gcpSecretManagerCertificateConfig)
          }
        }
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// GCPSecretManagerCertificateConfig configures a secret from
      /// [Secret Manager](https://cloud.google.com/secret-manager).
      public struct GCPSecretManagerCertificateConfig: Codable, Equatable, GoogleWKT._AnyPackable,
        Sendable
      {
        /// Secret URI, in the form
        /// "projects/$PROJECT_ID/secrets/$SECRET_NAME/versions/$VERSION".
        /// Version can be fixed (e.g. "2") or "latest"
        public var secretUri: Swift.String = Swift.String()

        @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

        /// Initialize a new instance of `GCPSecretManagerCertificateConfig`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = GCPSecretManagerCertificateConfig().with { $0.secretUri = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        private struct CodingKeys: CodingKey {
          var stringValue: Swift.String
          var intValue: Swift.Int? { nil }
          init(stringValue: Swift.String) { self.stringValue = stringValue }
          init?(intValue: Swift.Int) { nil }

          static let secretUri = CodingKeys(stringValue: "secretUri")

          static let _knownKeys: Set<Swift.String> = [
            "secretUri"
          ]
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.container(keyedBy: CodingKeys.self)
          if let value = try container.decodeIfPresent(Swift.String.self, forKey: .secretUri) {
            self.secretUri = value
          }
          for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
            self._unknownFields.json[key.stringValue] = try container.decode(
              GoogleWKT.Value.self, forKey: key)
          }
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.container(keyedBy: CodingKeys.self)
          try container.encode(self.secretUri, forKey: .secretUri)
          for (key, value) in self._unknownFields.json {
            try container.encode(value, forKey: CodingKeys(stringValue: key))
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.container.v1.ContainerdConfig.PrivateRegistryAccessConfig.CertificateAuthorityDomainConfig.GCPSecretManagerCertificateConfig"
        }
        public init(fromAny any: GoogleWKT.`Any`) throws {
          self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleWKT.Struct {
          return try GoogleWKT._slowAnySerialize(message: self)
        }
      }

      /// Certificate access config. The following are supported:
      /// - GCPSecretManagerCertificateConfig
      public enum OneOf_CertificateConfig: Codable, Equatable, Sendable {
        /// Secret Manager certificate configuration.
        indirect case gcpSecretManagerCertificateConfig(
          ContainerdConfig.PrivateRegistryAccessConfig.CertificateAuthorityDomainConfig
            .GCPSecretManagerCertificateConfig?)
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.container.v1.ContainerdConfig.PrivateRegistryAccessConfig.CertificateAuthorityDomainConfig"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.container.v1.ContainerdConfig.PrivateRegistryAccessConfig"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Defines writable cgroups configuration.
  public struct WritableCgroups: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Optional. Whether writable cgroups is enabled.
    public var enabled: Swift.Bool = Swift.Bool()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `WritableCgroups`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = WritableCgroups().with { $0.enabled = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let enabled = CodingKeys(stringValue: "enabled")

      static let _knownKeys: Set<Swift.String> = [
        "enabled"
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .enabled) {
        self.enabled = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.enabled, forKey: .enabled)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.container.v1.ContainerdConfig.WritableCgroups"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// RegistryHostConfig configures the top-level structure for a single
  /// containerd registry server's configuration, which represents one hosts.toml
  /// file on the node. It will override the same fqdns in
  /// PrivateRegistryAccessConfig.
  public struct RegistryHostConfig: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Defines the host name of the registry server, which will be used to
    /// create configuration file as /etc/containerd/hosts.d/<server>/hosts.toml.
    /// It supports fully qualified domain names (FQDN) and IP addresses:
    /// Specifying port is supported, while scheme and path are NOT supported.
    /// Wildcards are NOT supported.
    /// Examples:
    /// - `my.customdomain.com`
    /// - `10.0.1.2:5000`
    public var server: Swift.String = Swift.String()

    /// HostConfig configures a list of host-specific configurations for the
    /// server.
    /// Each server can have at most 10 host configurations.
    public var hosts: [ContainerdConfig.RegistryHostConfig.HostConfig] = []

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `RegistryHostConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RegistryHostConfig().with { $0.server = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let server = CodingKeys(stringValue: "server")
      static let hosts = CodingKeys(stringValue: "hosts")

      static let _knownKeys: Set<Swift.String> = [
        "server",
        "hosts",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .server) {
        self.server = value
      }
      if let value = try container.decodeIfPresent(
        [ContainerdConfig.RegistryHostConfig.HostConfig].self, forKey: .hosts)
      {
        self.hosts = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.server, forKey: .server)
      try container.encode(self.hosts, forKey: .hosts)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    /// CertificateConfig configures certificate for the registry.
    public struct CertificateConfig: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// One of the methods to configure the certificate.
      public var certificate: OneOf_Certificate? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `CertificateConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = CertificateConfig().with { $0.gcpSecretManagerSecretUri = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let gcpSecretManagerSecretUri = CodingKeys(stringValue: "gcpSecretManagerSecretUri")

        static let _knownKeys: Set<Swift.String> = [
          "gcpSecretManagerSecretUri"
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var certificate: OneOf_Certificate? = nil
        let certificateCheckAndSet = {
          if certificate != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'certificate'"))
          }
          certificate = $0
        }
        if let gcpSecretManagerSecretUri = try container.decodeIfPresent(
          Swift.String.self, forKey: .gcpSecretManagerSecretUri)
        {
          try certificateCheckAndSet(.gcpSecretManagerSecretUri(gcpSecretManagerSecretUri))
        }
        self.certificate = certificate
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.certificate {
          switch choice {
          case .gcpSecretManagerSecretUri(let value):
            try container.encode(value, forKey: .gcpSecretManagerSecretUri)
          }
        }
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      /// One of the methods to configure the certificate.
      public enum OneOf_Certificate: Codable, Equatable, Sendable {
        /// The URI configures a secret from
        /// [Secret Manager](https://cloud.google.com/secret-manager)
        /// in the format
        /// "projects/$PROJECT_ID/secrets/$SECRET_NAME/versions/$VERSION" for
        /// global secret or
        /// "projects/$PROJECT_ID/locations/$REGION/secrets/$SECRET_NAME/versions/$VERSION"
        /// for regional secret. Version can be fixed (e.g. "2") or "latest"
        case gcpSecretManagerSecretUri(Swift.String)
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.container.v1.ContainerdConfig.RegistryHostConfig.CertificateConfig"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// CertificateConfigPair configures pairs of certificates, which is used for
    /// client certificate and key pairs under a registry.
    public struct CertificateConfigPair: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Cert configures the client certificate.
      public var cert: ContainerdConfig.RegistryHostConfig.CertificateConfig? = nil

      /// Key configures the client private key. Optional.
      public var key: ContainerdConfig.RegistryHostConfig.CertificateConfig? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `CertificateConfigPair`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = CertificateConfigPair().with { $0.cert = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let cert = CodingKeys(stringValue: "cert")
        static let key = CodingKeys(stringValue: "key")

        static let _knownKeys: Set<Swift.String> = [
          "cert",
          "key",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.cert = try container.decodeIfPresent(
          ContainerdConfig.RegistryHostConfig.CertificateConfig.self, forKey: .cert)
        self.key = try container.decodeIfPresent(
          ContainerdConfig.RegistryHostConfig.CertificateConfig.self, forKey: .key)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.cert, forKey: .cert)
        try container.encodeIfPresent(self.key, forKey: .key)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.container.v1.ContainerdConfig.RegistryHostConfig.CertificateConfigPair"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// RegistryHeader configures headers for the registry.
    public struct RegistryHeader: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Key configures the header key.
      public var key: Swift.String = Swift.String()

      /// Value configures the header value.
      public var value: [Swift.String] = []

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `RegistryHeader`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = RegistryHeader().with { $0.key = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let key = CodingKeys(stringValue: "key")
        static let value = CodingKeys(stringValue: "value")

        static let _knownKeys: Set<Swift.String> = [
          "key",
          "value",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .key) {
          self.key = value
        }
        if let value = try container.decodeIfPresent([Swift.String].self, forKey: .value) {
          self.value = value
        }
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.value, forKey: .value)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.container.v1.ContainerdConfig.RegistryHostConfig.RegistryHeader"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// HostConfig configures the registry host under a given Server.
    public struct HostConfig: Codable, Equatable, GoogleWKT._AnyPackable,
      Sendable
    {
      /// Host configures the registry host/mirror.
      /// It supports fully qualified domain names (FQDNs) and IP addresses.
      /// Specifying scheme, port or path is supported. Scheme can only be http
      /// or https.
      /// Wildcards are NOT supported.
      /// Examples:
      /// - `my.customdomain.com`
      /// - `https://my.customdomain.com/path`
      /// - `10.0.1.2:5000`
      public var host: Swift.String = Swift.String()

      /// Capabilities represent the capabilities of the registry host,
      /// specifying what operations a host is capable of performing.
      /// If not set, containerd enables all capabilities by default.
      public var capabilities: [ContainerdConfig.RegistryHostConfig.HostCapability] = []

      /// OverridePath is used to indicate the host's API root endpoint is
      /// defined in the URL path rather than by the API specification. This may
      /// be used with non-compliant OCI registries which are missing the /v2
      /// prefix.
      /// If not set, containerd sets default false.
      public var overridePath: Swift.Bool = Swift.Bool()

      /// Header configures the registry host headers.
      public var header: [ContainerdConfig.RegistryHostConfig.RegistryHeader] = []

      /// CA configures the registry host certificate.
      public var ca: [ContainerdConfig.RegistryHostConfig.CertificateConfig] = []

      /// Client configures the registry host client certificate and key.
      public var client: [ContainerdConfig.RegistryHostConfig.CertificateConfigPair] = []

      /// Specifies the maximum duration allowed for a connection attempt to
      /// complete. A shorter timeout helps reduce delays when falling back to
      /// the original registry if the mirror is unreachable.
      /// Maximum allowed value is 180s. If not set, containerd sets default 30s.
      /// The value should be a decimal number of seconds with an `s` suffix.
      public var dialTimeout: GoogleWKT.Duration? = nil

      @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

      /// Initialize a new instance of `HostConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = HostConfig().with { $0.host = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private struct CodingKeys: CodingKey {
        var stringValue: Swift.String
        var intValue: Swift.Int? { nil }
        init(stringValue: Swift.String) { self.stringValue = stringValue }
        init?(intValue: Swift.Int) { nil }

        static let host = CodingKeys(stringValue: "host")
        static let capabilities = CodingKeys(stringValue: "capabilities")
        static let overridePath = CodingKeys(stringValue: "overridePath")
        static let header = CodingKeys(stringValue: "header")
        static let ca = CodingKeys(stringValue: "ca")
        static let client = CodingKeys(stringValue: "client")
        static let dialTimeout = CodingKeys(stringValue: "dialTimeout")

        static let _knownKeys: Set<Swift.String> = [
          "host",
          "capabilities",
          "overridePath",
          "header",
          "ca",
          "client",
          "dialTimeout",
        ]
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        if let value = try container.decodeIfPresent(Swift.String.self, forKey: .host) {
          self.host = value
        }
        if let value = try container.decodeIfPresent(
          [ContainerdConfig.RegistryHostConfig.HostCapability].self, forKey: .capabilities)
        {
          self.capabilities = value
        }
        if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .overridePath) {
          self.overridePath = value
        }
        if let value = try container.decodeIfPresent(
          [ContainerdConfig.RegistryHostConfig.RegistryHeader].self, forKey: .header)
        {
          self.header = value
        }
        if let value = try container.decodeIfPresent(
          [ContainerdConfig.RegistryHostConfig.CertificateConfig].self, forKey: .ca)
        {
          self.ca = value
        }
        if let value = try container.decodeIfPresent(
          [ContainerdConfig.RegistryHostConfig.CertificateConfigPair].self, forKey: .client)
        {
          self.client = value
        }
        self.dialTimeout = try container.decodeIfPresent(
          GoogleWKT.Duration.self, forKey: .dialTimeout)
        for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
          self._unknownFields.json[key.stringValue] = try container.decode(
            GoogleWKT.Value.self, forKey: key)
        }
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.host, forKey: .host)
        try container.encode(self.capabilities, forKey: .capabilities)
        try container.encode(self.overridePath, forKey: .overridePath)
        try container.encode(self.header, forKey: .header)
        try container.encode(self.ca, forKey: .ca)
        try container.encode(self.client, forKey: .client)
        try container.encodeIfPresent(self.dialTimeout, forKey: .dialTimeout)
        for (key, value) in self._unknownFields.json {
          try container.encode(value, forKey: CodingKeys(stringValue: key))
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.container.v1.ContainerdConfig.RegistryHostConfig.HostConfig"
      }
      public init(fromAny any: GoogleWKT.`Any`) throws {
        self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleWKT.Struct {
        return try GoogleWKT._slowAnySerialize(message: self)
      }
    }

    /// HostCapability configures capabilities for the registry host.
    ///
    /// - Note: Adding cases to this enumeration is not considered a breaking change.
    ///   Always include an `@unknown default:` case when switching over this type.
    ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
    ///   expecting specific values to remain unparsed; future releases may promote
    ///   them to named cases.
    public enum HostCapability: Codable, Equatable, Sendable {
      /// UNKNOWN should never be set.
      case unspecified
      /// Pull represents the capability to fetch manifests and blobs by digest.
      case pull
      /// Resolve represents the capability to fetch manifests by name.
      case resolve
      /// Push represents the capability to push blobs and manifests.
      case push
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown value is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      ///
      /// - Warning: Do not pattern-match specific integer values in this case;
      ///   future releases may promote them to named enum cases.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown value is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      ///
      /// - Warning: Do not pattern-match specific string literals in this case;
      ///   future releases may promote them to named enum cases.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .pull: return 1
        case .resolve: return 2
        case .push: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "HOST_CAPABILITY_UNSPECIFIED"
        case .pull: return "HOST_CAPABILITY_PULL"
        case .resolve: return "HOST_CAPABILITY_RESOLVE"
        case .push: return "HOST_CAPABILITY_PUSH"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:HostCapability/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "HOST_CAPABILITY_UNSPECIFIED": self = .unspecified
        case "HOST_CAPABILITY_PULL": self = .pull
        case "HOST_CAPABILITY_RESOLVE": self = .resolve
        case "HOST_CAPABILITY_PUSH": self = .push
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:HostCapability/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .pull
        case 2: self = .resolve
        case 3: self = .push
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode("HOST_CAPABILITY_UNSPECIFIED")
        case .pull: return try container.encode("HOST_CAPABILITY_PULL")
        case .resolve: return try container.encode("HOST_CAPABILITY_RESOLVE")
        case .push: return try container.encode("HOST_CAPABILITY_PUSH")
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.container.v1.ContainerdConfig.RegistryHostConfig"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.container.v1.ContainerdConfig"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
